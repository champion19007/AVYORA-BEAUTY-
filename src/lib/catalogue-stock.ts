/**
 * Kept for existing imports. Availability now lives with the rest of the
 * storefront's display data, where it is cached and invalidated alongside
 * prices: see `modules/catalog/storefront-data.ts`.
 */
export { catalogueStock as stockForCatalogue } from '@/modules/catalog/storefront-data';
